export default function ModelsPage() {
  return (
    <p className="text-sm text-muted-foreground">
      Phase 2 实施 Provider 配置后,这里展示 Multimodal LLM 与 ImageGen 的 provider 卡片(CRUD + Test Connection + 双向 mask)。
    </p>
  )
}
